#include <stdio.h>

int main(void)
{
    printf("Hello, World!\n");
}

// Toda vez que for rodar a aplicação, executar o "make.nomeDoArquivo" e depois "./nomeDoArquivo".
// Caso não for o nome do arquivo vai dar erro.